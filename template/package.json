{
    "name": "@red5/template",
    "version": "1.0.2",
    "description": "Template manager for the red5 http server.",
    "main": "lib/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prepack": "rm -rf lib && rm -rf types && tsc -p ."
    },
    "types": "types/index.d.ts",
    "author": "",
    "license": "ISC",
    "dependencies": {
        "html-minifier": "^3.5.15",
        "jsdom": "^12.0.0"
    },
    "devDependencies": {
        "@types/html-minifier": "^3.5.2",
        "@types/jsdom": "^11.12.0"
    },
    "files": [
        "lib",
        "types"
    ]
}
